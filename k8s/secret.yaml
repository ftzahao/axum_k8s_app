# 敏感配置：DATABASE_URL。
# ⚠️ 模板文件，勿直接 kubectl apply（占位 base64 连不上库）。
# 部署时用 `kubectl create secret`（见 k8s/README.md §3），或先替换 base64 值：
#   echo -n 'postgresql://user:password@postgres-host:5432/app' | base64
# 生产环境建议用 SealedSecret / External Secrets Operator / Vault 管理。
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: app
type: Opaque
data:
  DATABASE_URL: "cG9zdGdyZXNxbDovL2FwcDpwYXNzd29yZEBwb3N0Z3Jlcy1ob3N0OjU0MzIvYXBw"
